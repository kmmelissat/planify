# URL base de la API backend. En despliegue, configurar con la URL real
# (ej. https://api.planify.app). En local, si se omite, cae a http://localhost:8000.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000
